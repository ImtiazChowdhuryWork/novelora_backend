# Novelora Backend

Go REST API for the Novelora novel-reading platform. Serves the Flutter app (`novelora_app`) and the admin dashboard (`novelora_dashboard`).

## Stack

- Go (standard library `net/http`, Go 1.22+ method-pattern routing)
- PostgreSQL via `jackc/pgx/v5` (migrations embedded in the binary)
- `golang-jwt/jwt/v5` for access tokens, `x/crypto/bcrypt` for password hashing

## Project Structure

```
novelora_backend/
├── cmd/
│   └── server/
│       └── main.go              # Entry point: config → db → wiring → routes
├── internal/
│   ├── config/                  # Env / .env configuration loading
│   ├── database/                # Connection pool + embedded SQL migrations
│   │   └── migrations/          # 0001_create_users.sql, 0002_create_refresh_tokens.sql
│   ├── repository/              # SQL access (users, refresh tokens)
│   ├── service/                 # Business logic (auth: bcrypt, JWT, token rotation)
│   ├── handler/                 # Thin HTTP handlers, JSON in/out
│   └── middleware/              # Request logging
├── .env.example                 # Template — copy to .env
├── go.mod
└── README.md
```

## Setup & Run

1. PostgreSQL must be running with a `novelora` database/role (local dev uses PostgreSQL 17).
2. Copy `.env.example` to `.env` and set `DATABASE_URL` + `JWT_SECRET`.
3. Migrations run automatically at startup.

```bash
go run ./cmd/server
# listens on :8080 (override with PORT env var)
```

If the local Postgres Windows service can't start without admin rights, run it directly:

```powershell
& "C:\Program Files\PostgreSQL\17\bin\pg_ctl.exe" start -D "C:\Program Files\PostgreSQL\17\data"
```

### Running tests

```bash
go test ./...
```

Tests using `internal/testsupport` are integration tests against the real dev database from step 1/2 above — they create their own throwaway rows (unique per run) and clean up after themselves via `t.Cleanup`, but they do need that database up and reachable. A test skips itself (not a failure) if `DATABASE_URL`/`JWT_SECRET` aren't resolvable.

## Endpoints

| Method | Path                    | Status | Notes                                        |
|--------|-------------------------|--------|----------------------------------------------|
| GET    | `/health`               | ✅     | Liveness check                               |
| POST   | `/api/v1/auth/register` | ✅     | `{username, email, password}` → 201 + tokens |
| POST   | `/api/v1/auth/login`    | ✅     | `{email, password}` → 200 + tokens           |
| POST   | `/api/v1/auth/google`   | ✅     | `{id_token}` → 200 + tokens (503 if GOOGLE_CLIENT_ID unset) |
| POST   | `/api/v1/auth/refresh`  | ✅     | `{refresh_token}` → 200, rotates the token   |
| POST   | `/api/v1/auth/logout`   | ✅     | `{refresh_token}` → 204, revokes the token   |
| GET    | `/api/v1/users/me`      | ✅     | Bearer JWT → 200 current user, now includes `is_author`/`pen_name` (401 otherwise)|
| POST   | `/api/v1/users/me/author-profile` | ✅ | Bearer JWT; `{pen_name}` → 201 + rotated tokens (the new access token's `is_author` claim flips immediately). 409 if the caller already has an author profile |
| PUT    | `/api/v1/users/me/device-token` | ✅ | `{token, platform}` → 204; registers an FCM token for push |
| GET    | `/ws`                   | ✅     | Realtime events; JWT via `?token=` (WS upgrade)|
| *      | `/api/v1/admin/novels…` | ✅     | Admin-only novels CRUD + cover upload (list/create/get/update/soft-delete) |
| *      | `/api/v1/admin/novels/{id}/chapters…` | ✅ | Admin-only chapter create/import/list; `/admin/chapters/{id}` get/update/status/delete |
| *      | `/api/v1/author/novels…` | ✅ | Requires an author profile (`is_author` JWT claim); own-novels-only CRUD + cover upload — no delete, no reorder, editorial flags (`is_recommended`/`is_exclusive`/`rating`/`view_count`) always zeroed or preserved, never author-writable |
| *      | `/api/v1/author/novels/{id}/chapters…` | ✅ | Requires an author profile and ownership of the novel; `/author/chapters/{id}` get/update/status/schedule/delete, all ownership-checked via the chapter's novel |
| GET    | `/api/v1/novels…`, `/api/v1/chapters/{id}` | ✅ | Public catalog — published content only |
| POST   | `/api/v1/novels/{id}/report` | ✅ | Multipart: `reason`, `details`, optional `chapter_id`, up to 3 `images` files → 201; requires login. `reason` ∈ spam/plagiarism/inappropriate/harassment/broken/other; `details` required only for `other`; `chapter_id` must belong to the novel |
| DELETE | `/api/v1/reports/{id}` | ✅ | Requires login; own report only (ownership enforced by the query, not a 403 — a report id that isn't yours reads as 404). Withdraws the caller's own report; publishes `report.updated` |
| GET    | `/api/v1/users/me/reports` | ✅ | Bearer JWT → the caller's own report history, paginated; backs the app's "My Reports" Profile page |
| GET    | `/api/v1/admin/reports` | ✅ | Admin-only; `?status=&page=&page_size=`, paginated. Rows carry `image_count` (cheap correlated count) but leave `images` empty — only the single-report `GET` below fetches evidence URLs, avoiding an N+1 on the list |
| GET    | `/api/v1/admin/reports/{id}` | ✅ | Admin-only; full report record + evidence image URLs — backs the dashboard's report detail drawer |
| PUT    | `/api/v1/admin/reports/{id}/status` | ✅ | Admin-only; `{status, note}` ∈ pending/reviewed/dismissed → 200, audit-logged. Moving off `pending` writes a single-recipient inbox notification (+ best-effort push) to the reporting reader — the note verbatim if given, else a generic acknowledgement (see `NovelReportService.notifyReporter`) |
| GET    | `/api/v1/admin/authors/{authorName}/novels` | ✅ | Admin-only; `?exclude=<novelId>` — an author's other published novels, keyed by the free-text `author_name` string (no author-account system yet) |
| GET    | `/api/v1/admin/authors/{authorName}/strikes` | ✅ | Admin-only; strike history recorded against an author name |
| POST   | `/api/v1/admin/authors/{authorName}/strikes` | ✅ | Admin-only; `{note}` → 201, audit-logged |
| POST   | `/api/v1/admin/authors/{authorName}/hide-novels` | ✅ | Admin-only; soft-deletes every novel by that author name in one action → `{hidden_count}`, audit-logged, publishes `novel.deleted` per hidden novel (same event a single-novel delete fires) |

### Novel list filtering (`GET /admin/novels`, `GET /novels`)

Both list endpoints share `NovelRepository.List`/`NovelListFilter` (`internal/repository/novel_repository.go`), but expose different query params:

| Param | Admin (`/admin/novels`) | Public (`/novels`) | Notes |
|---|---|---|---|
| `search` | ✅ | — | Matches `title`/`author_name` **and** any assigned genre/tag name via an `EXISTS` subquery (added 2026-07-23 — searching "Comedy" now surfaces Comedy-tagged novels, not just literal title matches) |
| `sort` | ✅ (`views`/`rating`/`new`) | ✅ | `""` = manual `sort_order` |
| `genre_id` | — | ✅ | Single-value filter, joins `novel_genres` once — this is what the app's `GetNovelsUseCase` uses everywhere (Discover sections, category tabs) |
| `genre_ids` + `genre_match` | ✅ | — | Comma-separated ids, `genre_match=any` (OR, default) or `all` (AND) — dashboard-only multi-select filter, independent of `genre_id` |
| `status` | ✅ | ✅ | `ongoing`/`completed` |
| `recommended` | — | ✅ | Backs Readers' Choice-style filters app-wide |

`genre_id` (singular) and `genre_ids` (plural) are deliberately separate filter paths in `NovelListFilter` — the admin dashboard's multi-select never touches the public API's single-genre join, so extending one can't silently break the other.

Success responses: `{user: {id, username, email, created_at}, access_token, refresh_token, expires_in}`.
Errors: `{"error": "message"}` with 400 (validation/bad JSON), 401 (bad credentials / bad refresh token), 409 (email/username taken).

Paths mirror `ApiEndpoints` in the Flutter app (`lib/core/network/api_endpoints.dart`), which points at `http://<host>:8080`.

## Auth Design

- Access token: HS256 JWT (`sub` = user id), 15-minute TTL.
- Refresh token: opaque 32-byte random hex, 30-day TTL, stored as SHA-256 hash; rotated on every refresh and revoked on logout (reuse of an old token returns 401).
- Validation limits mirror the app's `AppConstants` (username 3–50, password 8–128).

## Push Notifications

Three independent triggers, all broadcasting to every registered device token (no per-novel/per-genre
"following" concept yet — see `internal/push`). Disabled gracefully until configured:

1. Firebase Console → Project Settings → Service Accounts → Generate new private key
2. Save the JSON file somewhere outside the repo and set `FIREBASE_CREDENTIALS_JSON=<path>` in `.env`
3. Restart the server — logs `push: FCM notifications enabled` instead of the no-op message

**What triggers a push** (added to 2026-07-23; see `internal/service/novel_service.go` and `ranking_notification_service.go`):

1. **Chapter publish** — unchanged, see below.
2. **Novel created** (`NovelService.Create` → `notifyNewNovelCreated`) — fires the moment a novel is saved via the dashboard's real API, even before it has any published chapters. Deliberately does *not* fire for novels inserted directly via SQL (bulk seeding) — only the real `POST /admin/novels` path triggers it.
3. **Novel enters a tracked ranked section** (`RankingNotificationService.DetectAndNotify`, run by a 45-minute ticker in `main.go`) — periodically diffs each registered section's current top-N against `novel_section_memberships` (new table, migration `0016`) and notifies only on genuinely new entries, never for a novel still sitting in a section it was already in. The section list (`defaultRankingSections` in `ranking_notification_service.go`) currently tracks Trending (global) and Comedy's Most Read; adding a future category's section is one list entry, not new code. Multiple novels entering the same section in one check are batched into a single push (`NotifyBroadcast`, no specific novel to deep-link to) rather than one push per novel — a single-entry check still deep-links straight to that novel (`NotifyNovelHighlight`). The in-app inbox always gets one row per novel regardless, so full detail survives even when the push itself is a digest.
   - **Cold-start note**: the very first time this ticker runs, every novel currently in a tracked section counts as a "new" entry (nothing was recorded before), so expect a one-time notification burst rather than a trickle. Every run after that only fires for genuine new entries.

## Production Hardening (2026-08-14)

Three fixes shipped ahead of real (non-`localhost`) traffic — see `internal/middleware/ratelimit.go`, `scripts/backup.sh`, and `internal/repository/outbox_repository.go`/`internal/service/outbox_processor.go`. A fourth candidate, S3-style persistent upload storage, needed no code change: the planned production host (a Hostinger KVM2 VPS) is a real VM with a persistent disk, so local-disk `uploads/` works as-is.

**Rate limiting** — `IPRateLimiter` (`golang.org/x/time/rate`, one token bucket per client IP, evicted after 3 minutes idle). A loose global limiter (300 req/min, burst 60) wraps every route; a strict one (10 req/min, burst 5) wraps just `/api/v1/auth/register|login|google|refresh` to blunt credential-stuffing. Reads `X-Forwarded-For`'s left-most address first, falling back to `RemoteAddr`, since production sits behind a reverse proxy. Over-limit requests get a `429` with `{"error":"too many requests, please slow down"}`.

**Backups** — `scripts/backup.sh`: `pg_dump` + `gzip` for the database, `tar -czf` for `uploads/`, pruning anything in `./backups/` older than `RETENTION_DAYS` (default 14). Reads `DATABASE_URL`/`UPLOADS_DIR` from `.env`, same as the server. Run manually or via cron, e.g. daily at 03:00:
```
0 3 * * * cd /path/to/novelora_backend && ./scripts/backup.sh >> backup.log 2>&1
```
Backups land on the same disk as the app for now — copying `./backups/` off-host (e.g. `rclone` to a free-tier Backblaze B2 bucket) is a documented follow-up, not built yet.

**Durable notifications (outbox pattern, migration `0031`)** — the two user-facing notification fan-outs (chapter published, novel created) used to run as fire-and-forget goroutines: if the process crashed mid-delivery, the notification was just gone, with no retry and no trace. They now write a row to `outbox_events` synchronously on the request path (a fast single insert, not the slow delivery work itself) instead of spawning a goroutine; a new ticker (`runOutboxProcessorTicker`, every 5s, same shape as `runScheduledPublishTicker`/`runRankingDetectionTicker`) claims pending rows and delivers them via `ChapterService.DeliverChapterPublishedNotification`/`NovelService.DeliverNovelCreatedNotification`, which re-fetch the chapter/novel fresh by ID rather than trusting a stale closure-captured snapshot. Failed deliveries increment `attempts`/`last_error` and retry on the next tick rather than vanishing. Deliberately **not** applied to `audit.Logger` — audit-log writes are a single fast local-DB insert with a narrow crash window and low stakes if occasionally lost, and routing them through the same 5s poll would add a visible delay to the admin Audit Log page for little benefit.

## Conventions

- Explicit names, no cryptic abbreviations (`responseWriter`, not `w` — project rule)
- Handlers stay thin; business logic lives in `internal/service/`, storage in `internal/repository/`
- Every endpoint returns JSON

## Next Steps

- [x] Auth middleware (validate Bearer JWT) for protected routes
- [x] Novels/chapters endpoints for the app's Discover, Library, and reader
- [x] Admin endpoints for the dashboard
- [x] Novel search broadened to genre/tag names; admin multi-select genre filter (`genre_ids`/`genre_match`)
- [x] Novel-created and ranked-section-entry push/inbox notifications (2026-07-23)
- [x] Reader reports (`novel_reports`, migration `0027`) — reader-facing create + admin list/moderate, `report.created`/`report.updated` realtime events, `PendingReports` on `/admin/stats` (2026-08-14)
- [x] Reader reports, extended same day: `resolution_note` (migration `0028`), `GET /users/me/reports`, `HasReported` (backs the app's `is_reported_by_me`), and per-reporter inbox notification + push on resolution reusing the existing single-user notification path (`NotificationRepository.CreateForUser`, `DeviceTokenRepository.ListTokensForUser` — both new, first targeted-not-broadcast notification methods in the codebase)
- [x] Reader reports, round 3 (migration `0029`, 2026-08-14): reports can now reference a specific chapter (`chapter_id`) and carry up to 3 evidence screenshots (`novel_report_images`, multipart upload reusing the image-upload package's content-sniffing helper); readers can withdraw their own report (`DELETE /reports/{id}`); the dashboard's report list is a click-through to a full detail view (`GET /admin/reports/{id}`) instead of an inline row-Select. New author-moderation panel (`AuthorModerationService`/`AuthorModerationHandler`), keyed by the free-text `author_name` string (no author-account system yet — see the migration's comment): an author's other published novels, a persistent strike history (`author_strikes`), and a one-click bulk-hide of every novel by that author name (soft-delete, publishes `novel.deleted` per novel so both the app and dashboard refresh immediately, same as a single novel delete)
- [x] **Author accounts, Phase 1 (migration `0030`, 2026-08-14):** "author" is an additive capability, not a role swap — `users.role` stays `reader`/`admin` untouched; a new `author_profiles` table is the source of truth, surfaced as an `is_author` JWT claim (alongside the existing `role` claim) and a new `RequireAuthor` middleware. `POST /users/me/author-profile` is the "become an author" upgrade (reissues tokens immediately via the existing `issueTokens`, so `is_author` flips without a re-login) — both the opt-in-from-an-existing-account and direct-signup (register, then this) paths use it, no separate registration endpoint needed. `novels` gained a nullable `owner_user_id` (every existing admin-uploaded novel stays `NULL`, `author_name` untouched either way); new author-scoped `/api/v1/author/novels...`/`/api/v1/author/chapters...` endpoints reuse the existing `NovelService`/`ChapterService` unchanged, with ownership enforced in the handler (404, not 403, for a novel/chapter that isn't yours — same non-leaking shape as the report feature's own-report-delete). Editorial flags (`is_recommended`, `is_exclusive`, the admin-typed `rating`, the manual `view_count` override) stay admin-only — the author-scoped handler zeroes them on create and preserves the existing value on every update regardless of request body. New sibling repo `novelora_author_dashboard` (React/Vite/TS/AntD, same stack as the admin dashboard) is the author-facing surface — see its own README for the frontend side. Verified end-to-end in a real browser: signed up, created a novel, published a chapter, confirmed it live in the public `GET /novels` API exactly like an admin-uploaded novel; verified a second author can't see or touch the first author's novel.
- [x] **Production hardening (2026-08-14):** rate limiting (`internal/middleware/ratelimit.go`), automated DB+uploads backups (`scripts/backup.sh`), and a durable Postgres outbox (migration `0031`) replacing the chapter-published/novel-created fire-and-forget goroutines — see "Production Hardening" above. Off-site backup sync (e.g. `rclone` to Backblaze B2) is a documented follow-up, not built yet.
- [ ] `RankingNotificationService`'s tracked section list only covers Trending + Comedy's Most Read — extend as more categories get their own dedicated sections
- [ ] No admin UI to manage the ranking section list or view push delivery success/failure — both are code-only today
- [x] First Go test coverage in this repo (2026-08-14): `internal/testsupport` is a new integration-test helper package — this codebase has no repository/service interfaces to fake behind, so tests run against the real dev Postgres pool (same `.env`/`config.Load()` the server itself uses; a small `runtime.Caller`-based lookup works around `go test` setting the working directory to the package under test, not the repo root). Covers the author-scoped novel handler's two highest-risk behaviors: ownership isolation (a second author gets 404, not 403, fetching/editing/listing another author's novel) and editorial-flag lockout (a crafted request setting `is_recommended`/`rating`/`view_count` is silently ignored) — plus `AuthService.BecomeAuthor` (profile creation, the 409-on-repeat case, empty-pen-name validation). Everything else in the repo is still untested; this is a first foothold and a reusable pattern, not full coverage.
- [x] **Author novel delete + bulk chapter import (2026-08-14):** a comparison against the admin dashboard's novel-upload flow found two real gaps for a self-service author: no way to withdraw a novel, and no way to bulk-import a manuscript (only one-chapter-at-a-time via the editor). Both were purely additive — `NovelService.Delete`/`ChapterService.Import` already took no ownership parameter, so `AuthorNovelHandler.Delete` (`DELETE /api/v1/author/novels/{id}`) and `AuthorChapterHandler.Import` (`POST /api/v1/author/novels/{id}/chapters/import`) just layer the existing `loadOwnedNovel` ownership check in front of the same service calls the admin handlers use, reusing the admin handlers' request/response shapes verbatim. See `novelora_author_dashboard/README.md` for the ported frontend wizard. Verified end-to-end in a real browser: imported a 3-chapter `.txt` manuscript into a fresh author's novel (correct chapter titles/word counts detected), then deleted the novel and confirmed `deleted_at` was set.
- [x] **Author profile editing (2026-08-15):** `author_profiles.bio` existed since migration `0030` but nothing ever wrote to it, and pen name was create-once. Added `AuthorProfileRepository.Update` + `UserHandler.UpdateAuthorProfile` (`PUT /api/v1/users/me/author-profile`, distinct from the existing `POST` at that path, which stays the one-time become-author create) — validates and writes both fields, no token reissue needed since neither is a JWT claim. `GET /users/me` (`respondWithUser`) now also returns `bio`. See `novelora_author_dashboard/README.md` for the new Profile page.
- [x] **Report moderation panel, real-accounts phase (migration `0032`, 2026-08-15):** the author-moderation panel (`AuthorModerationService`/`Handler`, `AuthorStrikeRepository`) was 100% keyed by the free-text `author_name` string since `author_strikes` (migration `0029`) predates author accounts. `author_strikes` gained a nullable `owner_user_id` (same `ON DELETE SET NULL` shape as `novels.owner_user_id`) and `NovelReportRepository` now also selects `n.owner_user_id`, so the report a moderator is reviewing carries it end-to-end. Every `AuthorModerationService` method now takes an optional `ownerUserID *string` alongside `authorName` and branches: account-keyed queries (`NovelRepository.ListByOwnerUserID`/`BulkHideByOwnerUserID`, `AuthorStrikeRepository.ListForOwner`/`CountForOwner`) when the reported novel has a real account, the original name-keyed queries otherwise (still the common case — existing strikes have no way to backfill an account, since they never captured one). Routes are unchanged (`/admin/authors/{authorName}/...`) plus an optional `?owner_user_id=` query param the frontend supplies when available. Verified end-to-end in the browser with two reports: one against an author-dashboard-created novel (panel shows a green "Verified account" badge, adding a strike correctly stamps `owner_user_id` in the database) and one against an admin-uploaded novel (panel shows "Unclaimed", behaves identically to before the change — other novels by `author_name`, no `owner_user_id`).
- [x] **Moderation workflow: targeted actions + author resubmission (migration `0033`, 2026-08-15):** closed two gaps in report moderation — admin could only set a report's status or bulk-hide an author's entire catalog (no way to act on just the reported novel/chapter), and the whole flow was one-directional (an actioned author had no way to see why or respond). `novel_reports` gained `author_response text`. The status lifecycle grew two values: `action_required` (admin-settable via `UpdateStatus`, requires a non-empty `resolution_note` — enforced both client- and server-side — and triggers a new `notifyAuthor` call, same inbox+push shape as the existing `notifyReporter`, skipped gracefully when the novel has no linked account) and `resubmitted` (deliberately **not** in `allowedReportStatuses` — reachable only through the new author-only `NovelReportService.Resubmit`, so an admin can never skip the author's own step). `Resubmit(ctx, reportID, callerUserID, message)` reuses the non-leaking ownership-404 pattern from `loadOwnedNovel` and requires the report currently be `action_required`; the repository's `Resubmit` sets `status = 'resubmitted'` + `author_response` while deliberately leaving `resolution_note`/`reviewed_by`/`reviewed_at` untouched, so the admin's original note stays visible next to the author's reply. A fresh `action_required` note (`UpdateStatus`'s non-pending branch) resets `author_response = ''` so a stale reply never lingers next to new instructions. New `GET /api/v1/author/reports` (`ListForOwner`) and `POST /api/v1/author/reports/{id}/resubmit` round out the author-facing API. `stats_repository.go`'s pending-reports count now covers `pending`, `action_required`, and `resubmitted`. The two "take action" buttons this unlocked (hide the reported novel, unpublish the reported chapter) needed **no new endpoints at all** — `DELETE /admin/novels/{id}` and `PUT /admin/chapters/{id}/status` already existed and were wired straight into a redesigned `ReportDetailDrawer.tsx`. See `novelora_author_dashboard/README.md` for the new author-facing Notices page. Verified end-to-end with throwaway accounts: action-required note enforced (rejected empty, accepted non-empty, author notified via a real `notifications` row) → Notices page shows it → resubmit persists `author_response` while preserving `resolution_note` → admin sees "Resubmitted for review" plus the author's response, sets "Reviewed" (confirmed `author_response` reset to `''` in the DB) → "Unpublish this chapter" confirmed (`chapters.status = 'draft'`) → "Hide this novel" confirmed (`novels.deleted_at` set).
- [x] **Report Details drawer: three-tab redesign + report history (2026-08-15):** the drawer from the previous entry bolted its author panel onto the bottom of a single long scroll. User feedback asked for it to feel genuinely comprehensive — a chapter, a novel, the whole catalog, and the author's record should all be one click away, plus a way to see an author's past reports before acting. No schema change; the one new capability is report history — `NovelReportRepository` gained `ListForAuthorName`/`ListForOwnerAll` (unpaginated, mirroring `AuthorStrikeRepository.ListForAuthor`/`ListForOwner`'s exact shape), `AuthorModerationService.Reports` branches on `ownerUserID` the same way `Strikes` already does, and `AuthorModerationHandler.Reports` (`GET /admin/authors/{authorName}/reports?owner_user_id=`) reuses `reportResponse` from the report handler verbatim. Everything else was reuse, not new endpoints: the dashboard's `ReportDetailDrawer.tsx` became a three-tab shell (`Tabs`) — **Reports** (the current report, take-action, resolve, plus the new report-history list, each row clickable to jump the drawer to that report without closing it), **Published** (new `PublishedNovelsPanel.tsx` — every novel by the author via the existing `GET /admin/authors/{authorName}/novels` endpoint called with `exclude=''`, since nothing has an empty id to exclude, so it returns the whole catalog instead of "other" novels; each row expands to its chapters via the existing `useChaptersQuery`, with per-chapter "Unpublish" and per-novel "Hide", plus "Hide all novels" moved up from the old bottom panel), and **Profile** (new `AuthorProfilePanel.tsx` — identity + strike history + add-strike, moved near-verbatim). Verified end-to-end in the admin browser (including creating a second throwaway report against an existing author mid-session to confirm the history list and its click-to-jump both worked, then deleting it) — a stale `go run` dev process from before this change had to be restarted to pick up the new route, a reminder that `main.go` route changes need a server restart, not just a rebuild.
- [x] **Reports: visual redesign + restore/republish actions (2026-08-15):** two rounds of follow-up on the drawer above. First, a pure visual pass on user feedback ("basic design, I didn't like it") — no functional change, just craft: a shared `Pill` primitive (real hex tints instead of AntD's named Tag colors) and `SectionPanel` (bounded, tinted zones instead of a flat divider-separated scroll) now carry every status/reason/identity badge across `ReportsPage.tsx` and the whole drawer; `AuthorIdentityHeader` (initial-letter avatar + name + verified pill) is shared between the Published and Profile tabs. Second, testing that redesign surfaced two real product gaps: no way to undo "Hide this novel"/"Unpublish this chapter" once clicked, and no signal that a resolve note is invisible to "Unclaimed" (no linked account) authors. `NovelRepository.Restore` (`UPDATE novels SET deleted_at = NULL ...`) mirrors `SoftDelete` exactly — its own doc comment had anticipated this ("recoverable by clearing deleted_at manually") but nothing had built it until now; `NovelService.Restore` publishes `novel.updated` (not `novel.deleted`, since nothing needs to disappear) and a new `POST /admin/novels/{id}/restore` completes the pair with `DELETE /admin/novels/{id}`. The report response gained two live-state fields — `chapter_status` (via the already-`LEFT JOIN`ed `c.status`) and `novel_hidden` (`n.deleted_at IS NOT NULL`) — so the drawer's Take Action buttons toggle instead of only ever offering the one-way action, plus an inline note appears when `owner_user_id` is null explaining the note is dashboard-only. One bug caught and fixed during live verification: `useDeleteNovel`/`useRestoreNovel`/`useUpdateChapterStatus` only invalidate their own domain's query cache, not the report detail query that `chapter_status`/`novel_hidden` live on — the toggle buttons silently didn't flip until the drawer manually invalidates `REPORTS_QUERY_KEY` after each action too. Also investigated, but couldn't reproduce, an earlier report of a status save appearing to leave a report on "Pending" — retested the exact flow twice against a stable server and it worked correctly both times; most likely explanation is the backend dev server restart that happened mid-session for the previous entry.
- [x] **Take Action → notify + republish-request loop (2026-08-15, no backend changes):** user pointed out that unpublishing/hiding via Take Action was silent — the author was never told, couldn't see why, and had no way to request republish. The `action_required` → `notifyAuthor` → author Notices → "Mark fixed — request re-review" (`Resubmit`) loop already existed for the Resolve section's status Select; this connects Take Action to it instead of building a second system. In `ReportDetailDrawer.tsx` (`novelora_dashboard`), the Resolve panel now renders before Take Action (the note is the reason Take Action uses), and hiding/unpublishing is disabled until a note is typed; on confirm it performs the content mutation *and* calls the existing `UpdateStatus(status: 'action_required', note)`, which already notifies the author when they have a linked account. Restoring/republishing stays a plain toggle, no note required. On the author side (`novelora_author_dashboard`), `Notice` gained `chapter_status`/`novel_hidden` — fields the `ListForOwner` endpoint already returned (added for the admin drawer's live-state toggles two entries up) but the frontend type never read — so `NoticesPage.tsx` can show a "Chapter unpublished"/"Novel hidden" tag next to the note, not just the note text. No backend code changed at all this round. Verified end-to-end with a throwaway author/novel/published-chapter/report: unpublished via Take Action with a note → toast confirmed notification → author's Notices showed the note, the "Chapter unpublished" tag, and "Mark fixed — request re-review" → resubmitted with a reply → confirmed server-side (`GET /author/reports`) the report reached `status: "resubmitted"` with both `resolution_note` and `author_response` intact.
- [x] **Moderation workflow v2: full Reporter/Admin/Author state machine (migration `0034`, 2026-08-15):** the user handed over a complete spec for a proper three-party moderation workflow, closing gaps the incremental rounds above hadn't reached — no notification on report *submission*, no neutral "your content was reported" heads-up to the author independent of any hold decision, no per-report action audit trail, and no evidence upload on the author's fix-and-resubmit step. Implemented as a real hand-rolled state machine rather than a generic status setter: `submitted -(auto, first admin open)-> under_review -> rejected [terminal] | chapter_on_hold | novel_on_hold -> pending_release_review (author submits a release request) -> resolved [terminal]` (approve) or back to whichever hold status it came from (reject, with the admin's comment living on the release request, not a new top-level status). `under_review -> resolved` directly (no hold) is an addition beyond the literal spec, for reports an admin handles out-of-band. Two new tables: `moderation_actions` (id, report_id, action_type, admin_id, notes, created_at — the per-report audit trail `audit_logs` couldn't provide, no `entity_id` filter there) and `release_requests` + `release_request_images` (the author's explanation + up to 3 proof screenshots, mirroring `novel_report_images`'s exact shape) — replacing the old bare-message `Resubmit`. The migration also **data-migrates every existing report row** to the new vocabulary (`pending`→`submitted`, `action_required`→`chapter_on_hold`/`novel_on_hold` inferred from whether `chapter_id` was set, `resubmitted`→`pending_release_review`, `reviewed`→`resolved`, `dismissed`→`rejected`) rather than leaving old data on a stale enum. `NovelReportService` gained one method per transition (`MarkUnderReview` private/auto, `Reject`, `HoldChapter`/`HoldNovel`, `ResolveDirect`, `SubmitReleaseRequest`, `ApproveRelease`, `RejectRelease`) — `HoldChapter`/`HoldNovel` now perform the actual takedown (via the same `ChapterService.UpdateStatus`/`NovelService.Delete` every other unpublish/hide already uses) atomically with the status transition and audit-log write, and `ApproveRelease` atomically restores whichever content is on hold (checked via the report's own live `NovelHidden`/`ChapterStatus` fields) instead of requiring a separate manual click. New notify methods cover every step the spec lists: reporter gets "submitted", "under review", "rejected", and "resolved"; author gets a neutral "your content was reported" heads-up at `under_review` (new — previously authors only heard about reports bundled with a hold), plus hold/release-rejected/resolved notices. `internal/repository/novel_report_repository.go` gained `SetStatus` (a bare transition that doesn't touch `resolution_note`/`reviewed_by`, for the auto step and the author's own `SubmitReleaseRequest` — `UpdateStatus` stays reserved for genuine admin decisions). `stats_repository.go`/`CountPending` simplified to `status NOT IN ('resolved', 'rejected')` rather than enumerating every non-terminal value. See `novelora_dashboard/README.md` and `novelora_author_dashboard/README.md` for the redesigned admin Decision panel + History timeline and the author's evidence-bearing release-request form. Verified end-to-end with a fresh throwaway reader/author/novel/chapter: submit (reporter notified) → open as admin (auto under_review, both reporter and author notified, content still live) → Hold chapter with a note (chapter unpublished, author notified with the reason, `moderation_actions` row created) → author submits a release request with an explanation → admin Rejects with a comment (content stays down, report reverts to `chapter_on_hold` with the *original* hold note preserved, author sees the rejection comment) → author submits a second release request → admin Approves (chapter automatically republished, `resolved`, both author and reporter notified) — the full History timeline showed all five transitions in order throughout.
- [x] **Close the hold-bypass hole (2026-08-19):** user reported that an author could simply click Publish (or schedule a future auto-publish) on a chapter an admin had just held, walking straight around the hold — `HoldChapter` only ever drafts the chapter, indistinguishable at the chapter level from the author's own unpublish. `NovelReportRepository.HasActiveChapterHold` checks for an active `chapter_on_hold`/`pending_release_review` report; `AuthorChapterHandler.blockIfHeld` rejects `UpdateStatus(..., "published")` and `Schedule` (but not `Unschedule`/drafting) while true — deliberately placed at the handler layer, not inside the shared `ChapterService`, so the admin's own restore path and the scheduled-auto-publish ticker (both call `ChapterService` directly) are unaffected. Also found and fixed a second, more severe hole while investigating: a schedule set *before* a hold survives it (`UpdateStatus`'s SQL only clears `scheduled_at` on transition to `"published"`, not `"draft"`), so the background ticker would silently auto-publish held content with zero author action — `HoldChapter` now also calls `ChapterService.Unschedule` when it drafts the chapter. Verified live: disabled Publish/Schedule buttons + tooltip in the author dashboard, a direct API call against a held chapter returned 400 with the guard's message, and holding a chapter with a real pre-existing schedule (via the actual admin Hold flow) confirmed `scheduled_at` cleared in the database.
- [x] **Reports/Notifications split + soft delete (migration `0036`, 2026-08-19):** three-part user request. (1) The author dashboard's whole notification story ran through "Notices" — every report event already wrote a row into the generic `notifications` table (`NovelReportService.notify`, `/users/me/notifications`) but nothing in the author dashboard ever read it; that page did double duty as report-browser and de-facto notification feed. No backend change needed here — the inbox endpoints already existed, just unused; see `novelora_author_dashboard/README.md` for the new dedicated Notifications page built on top of them. (2) Admin had no way to remove a report at all. Added soft delete (`novel_reports.deleted_at`, `NovelReportRepository.SoftDelete`/`BulkSoftDeleteByStatus`/`PurgeDeletedBefore`) — refused via `NovelReportService.AdminDelete` while a report is `chapter_on_hold`/`novel_on_hold`/`pending_release_review` (deleting one of those would strand the hold with nothing left explaining it to the author), otherwise soft-deleted immediately and hard-purged 30 days later by a new daily ticker (`runReportPurgeTicker`, `main.go`). `List`/`ListForOwner`/`ListForUser`/`CountPending` all gained a `deleted_at IS NULL` filter; `ListForAuthorName`/`ListForOwnerAll` deliberately did not, since a deleted report is still useful context in the admin's "other reports against this author" history panel. `AdminBulkDelete` additionally clears an entire terminal status at once (resolved or rejected only — anything else either still needs attention or is actively holding content), so decluttering an old backlog doesn't mean deleting one row at a time. (3) New `GET /author/reports/{id}/moderation-actions` reuses the existing admin History timeline's data (minus admin identity — `authorModerationActionResponse` omits `AdminName`) for a new author-facing per-report timeline. Verified live: soft delete + block-while-held both confirmed via direct authenticated API calls (200/400 as expected) and the real admin Delete button; the author's moderation-actions endpoint confirmed to return real, correctly-shaped history for a report with a full hold→reject→approve cycle.
